<% content_for :title, "#{@product.name.titleize} - #{@product.kind.titleize} from Brasil Sul | " %>

<div class="row">
  <div class="col-md-8 col-xs-12">
    <div id="carousel-example-generic" class="carousel slide" data-ride="carousel" data-interval="false">
      <!-- Indicators -->
      <ol class="carousel-indicators">
        <li data-target="#carousel-example-generic" data-slide-to="0" class="active"></li>
        <li data-target="#carousel-example-generic" data-slide-to="1"></li>
      </ol>

      <!-- Wrapper for slides -->
      <div class="carousel-inner">
        <% @product.images.each_with_index do |image, index| %>
          <div class="item <%= 'active' if index == 0 %>">
            <%= cl_image_tag image.cloudinary_ref,
                  alt: "#{@product.name}",
                  height: 800,
                  width: 400,
                  crop: :pad,
                  format: :png,
                  class: 'img-responsive center-block'
            %>
          </div>
        <% end %>
      </div>

      <!-- Controls -->
      <a class="left carousel-control" href="#carousel-example-generic" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left"></span>
      </a>
      <a class="right carousel-control" href="#carousel-example-generic" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right"></span>
      </a>
    </div>
  </div>

  <div class="col-md-4 col-xs-12">
    <h1><%= @product.name.strip -%></h1>
    <p>from <span itemprop="brand">Brasil Sul</span></p>
    <% if @product.description %>
      <% @product.description.split("\n").each do |paragraph| %>
        <p><%= paragraph %></p>
      <% end %>
    <% end %>
    <h4>Fabric & Feel</h4>
    <p><%= @product.fabric_and_feel %></p>
    <h4>Fit & Size</h4>
    <p><%= @product.fit_and_size %></p>
    <h4>Measurements</h4>
    <p><%= @product.measurements %></p>
    <h4>Care Instructions</h4>
    <p>Cool wash only. Do not tumble dry.</p>
    <p>Made in Brazil, sweatshop free.</p>
    <%= link_to "Buy now", contact_path, class: "btn btn-default" %>
  </div>

</div>